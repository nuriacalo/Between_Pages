spring:
  application:
    name: backend

  datasource:
    url: jdbc:postgresql://localhost:5432/between_pages
    username: nuriacalo
    password:
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true
    open-in-view: false
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true

google:
  books:
    api-key: ${GOOGLE_BOOKS_API_KEY:}

jwt:
  secret: betweenpages_clave_secreta_muy_larga_para_que_sea_segura_2024
  expiration: 86400000

logging:
  level:
    com.calonuria.backend.service.catalog: INFO
    com.calonuria.backend.controller: INFO